import { colors } from "../theme/base"

const stackConfig = {
  headerShown: true,
  headerTintColor: colors.primary.onColor,
  headerStyle: {
    backgroundColor: colors.dark_2,
  },
  headerTitleStyle: {
    alignSelf: "center",
    color: colors.primary.onColor,
    fontWeight: "bold",
    fontSize: 18,
  },
}
export default stackConfig
